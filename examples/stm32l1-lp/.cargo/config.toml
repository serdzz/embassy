[target.'cfg(all(target_arch = "arm", target_os = "none"))']
# replace STM32L151CBxxA with your chip as listed in `probe-rs chip list`
runner = "probe-rs run --chip STM32L151RC       "

[build]
target = "thumbv7m-none-eabi"

[env]
DEFMT_LOG = "info"
